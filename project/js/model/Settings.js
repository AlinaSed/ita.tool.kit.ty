'use strict';

class Settings {
    constructor () {
        this.directionList = [];  
    }

    addDirection () {
        this.directionList.push(new Direction(name));
    }
}     

module.exports = Settings;
